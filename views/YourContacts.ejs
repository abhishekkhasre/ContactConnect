<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ContactList</title>
    <link rel="stylesheet" href="css/home.css">
    
</head>
<body>
<div class="Header">

    <div class="Logo">
        <h1><%= user.name%> Contact's</h1>
    </div>

    
    <div class="credentials">

        <a href="/"><button class="log">Home</button> </a> 
        <a href="/user/profile"><button class="reg">Profile</button></a>

    </div>

</div>

<div class="search-container">

    <form action="/updatecontact" method="post">
        <input type="email" name="email" placeholder="Enter Updated Email" required>
        <input type="submit" value="Update Email">
    </form>
   
</div>


<div class="search-container">

    <form action="/addcontact" method="post">
        <input type="text" name="name" placeholder="Enter Name" required>
        <input type="number" name="number" placeholder="Enter Number" required>
        <input type="submit" value="Add to contact">
    </form>
    
</div>

<div class="ContactListdiv">
    <h1>YOUR CONTACT LIST</h1>
    <% if(contacts)  {%> 
        <% for (c of contacts) { %> 
            <% for (All of Allcontacts) {%> 
                <% if(c.contactListId === All.id){ %> 
                    <div class="eachcontact">
                    <span><b>Name: &nbsp; </b><%=All.name %> &nbsp; &nbsp;</span> 
                    <span><b>Number: &nbsp; </b><%= All.number %>&nbsp;&nbsp; </span> 
                    <span><b>Status &nbsp; </b><% if(All.status){%>Verified <%}else{%> Not Verified <%} %>&nbsp;&nbsp; </span>
                    <span><a href="/addspamcount/<%=All.id%>"><button >Spam(<%=All.spam %>)</button></a></span>
                    <br>
                </div>
                <%}%> 
            <% } %>
        <% } %>
    <% } %>


</div>


</body>
</html>